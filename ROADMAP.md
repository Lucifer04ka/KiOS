# Полная дорожная карта KiOS

## 📍 Текущий статус (Фаза I — ЗАВЕРШЕНО)

```
Состояние: ✅ Рабочее ядро
- GDT, IDT, PIC, Long Mode
- PMM (Bitmap), Heap
- PS/2 Keyboard, Framebuffer Terminal
- Базовый shell
```

---

## 🟡 ФАЗА II: Виртуализация и Изоляция

**Цель:** Внедрить виртуальную память и разделить пространства ядра и пользователя.

### 2.1. Виртуальный менеджер памяти (VMM)

#### 2.1.1. Структуры данных
- [ ] **src/vmm.h** — заголовочный файл
- [ ] **src/vmm.c** — реализация
- [ ] **src/paging.h** — константы (PML4, PDPT, PD, PT)
- [ ] **src/paging.c** — макросы для работы со страницами

#### 2.1.2. Функции VMM
- [ ] `vmm_init()` — инициализация, создание kernel page tables
- [ ] `pml4_create()` — создание нового PML4
- [ ] `map_page(uint64_t virt, uint64_t phys, uint64_t flags)` — маппинг страницы
- [ ] `unmap_page(uint64_t virt)` — размаппинг
- [ ] `get_page_table_entry(uint64_t virt)` — получение PTE
- [ ] `vmm_alloc_range()` — выделение диапазона страниц

#### 2.1.3. Флаги доступа
- [ ] PAGE_PRESENT (0x01)
- [ ] PAGE_WRITABLE (0x02)
- [ ] PAGE_USER (0x04)
- [ ] PAGE_WRITE_THROUGH (0x08)
- [ ] PAGE_NO_CACHE (0x10)

#### 2.1.4. Интеграция с ядром
- [ ] Изменить `_start()` для включения paging (CR3)
- [ ] Перемаппировать ядро на высокие адреса (KERNEL_VMA = 0xFFFF800000000000)
- [ ] Обновить heap для работы с виртуальной памятью

### 2.2. Защита и привилегии

#### 2.2.1. Segment Descriptor Extension
- [ ] Расширить GDT для user segment selectors
- [ ] Добавить TSS (Task State Segment) для стека ядра
- [ ] `gdt_load_tss()` — загрузка TSS

#### 2.2.2. System Calls
- [ ] **src/syscall.h** — номера syscall
- [ ] **src/syscall.c** — обработчик
- [ ] `syscall_init()` — настройка IDT для вектора 0x80
- [ ] `syscall_handler()` — dispatch для user space

#### 2.2.3. Переключение контекста
- [ ] `enter_user_mode(uint64_t entry, uint64_t stack)` — переход в user mode
- [ ] Сохранение/восстановление регистров

### 2.3. Kernel API

#### 2.3.1. Базовые интерфейсы
- [ ] **include/kiosk.h** — главный заголовочный API
- [ ] `kiosk_print(const char*)` — вывод в терминал
- [ ] `kiosk_malloc(size_t)` / `kiosk_free(void*)`
- [ ] `kiosk_read_key()` — чтение клавиши
- [ ] `kiosk_get_meminfo()` — информация о памяти

#### 2.3.2. Error Handling
- [ ] **include/error.h** — коды ошибок
- [ ] `panic(const char* msg)` — kernel panic
- [ ] Логирование в framebuffer

---

## 🟠 ФАЗА III: Экосистема и Хранение

**Цель:** Добавить работу с накопителями и собственную файловую систему.

### 3.1. Драйверы накопителей

#### 3.1.1. AHCI/SATA Driver
- [ ] **src/driver/ahci.h**
- [ ] **src/driver/ahci.c**
- [ ] `ahci_init()` — определение контроллера
- [ ] `ahci_read(uint64_t lba, uint32_t count, void* buf)`
- [ ] `ahci_write(uint64_t lba, uint32_t count, void* buf)`
- [ ] Support для Lenovo G580 (BCM Crystal HD @ 14E4:4727)

#### 3.1.2. NVMe Driver (опционально)
- [ ] **src/driver/nvme.h**
- [ ] **src/driver/nvme.c**
- [ ] Если NVMe контроллер обнаружен

#### 3.1.3. PCI/ACPI
- [ ] **src/driver/pci.h** / **src/driver/pci.c**
- [ ] `pci_scan()` — сканирование шины
- [ ] `pci_read_bar()` — чтение BAR регистров

### 3.2. Файловая система (KiFS)

#### 3.2.1. Спецификация KiFS
- [ ] **docs/kifs-spec.md** — полная спецификация
- [ ] On-disk структура суперблока
- [ ] Инodes и метаданные
- [ ] Journal (для отказоустойчивости)

#### 3.2.2. Реализация KiFS
- [ ] **src/fs/kifs.h**
- [ ] **src/fs/kifs.c**
- [ ] `kifs_format()` — форматирование раздела
- [ ] `kifs_mount()` — монтирование
- [ ] `kifs_open()`, `kifs_read()`, `kifs_write()`, `kifs_close()`
- [ ] `kifs_mkdir()`, `kifs_rm()`, `kifs_ls()`

#### 3.2.3. VFS (Virtual File System)
- [ ] **src/fs/vfs.h** — абстрактный слой
- [ ] **src/fs/vfs.c**
- [ ] `vfs_mount(const char* device, const char* mountpoint)`
- [ ] `vfs_open(const char* path)` — универсальный интерфейс

### 3.3. Формат исполняемых файлов

#### 3.3.1. KiELF (KiOS Executable and Linkable Format)
- [ ] **docs/kielf-spec.md**
- [ ] Header (Magic: KELF)
- [ ] Sections: .text, .data, .bss, .rodata
- [ ] Symbol table
- [ ] Relocation entries

#### 3.3.2. Загрузчик ELF
- [ ] **src/elf.h**
- [ ] **src/elf.c**
- [ ] `elf_load(const char* path)` — загрузка в память
- [ ] `elf_validate_header()`
- [ ] Section mapping

### 3.4. Драйверы устройств

#### 3.4.1. PS/2 Mouse
- [ ] **src/driver/ps2mouse.h**
- [ ] **src/driver/ps2mouse.c**
- [ ] `mouse_init()` — инициализация
- [ ] `mouse_get_packet()` — получение данных
- [ ] Mouse interrupt (IRQ 12)

#### 3.4.2. Framebuffer Extensions
- [ ] **src/gfx/framebuf.h**
- [ ] Поддержка разрешений (VBE)
- [ ] `set_video_mode(uint16_t width, uint16_t height, uint8_t bpp)`
- [ ] 2D графика: линии, прямоугольники, круги

#### 3.4.3. RTC (Real Time Clock)
- [ ] **src/driver/rtc.h**
- [ ] `rtc_read_time()` — чтение времени
- [ ] `rtc_read_date()` — чтение даты
- [ ] Будильники и прерывания

---

## 🔴 ФАЗА IV: Язык и Рантайм

**Цель:** Создать собственный язык программирования и рантайм.

### 4.1. Спецификация языка (KiLang)

#### 4.1.1. Язык дизайн
- [ ] **docs/kilang-spec.md**
- [ ] Лексические токены (keywords, operators, literals)
- [ ] Синтаксис (BNF или EBNF)
- [ ] Система типов (primitives, structs, enums)
- [ ] Memory model (ownership?)

#### 4.1.2. Основные конструкции
- [ ] Переменные и константы
- [ ] Функции и параметры
- [ ] Управление потоком (if/else, loops, match)
- [ ] Structs и enums
- [ ] Pattern matching
- [ ] Error handling (Result?)

### 4.2. Компилятор (KiCC)

#### 4.2.1. Frontend
- [ ] **compiler/scanner.l** — лексер
- [ ] **compiler/parser.y** — парсер (yacc/bison или ручной)
- [ ] AST (Abstract Syntax Tree) определения

#### 4.2.2. Backend
- [ ] **compiler/codegen.c** — генерация кода
- [ ] KiELF emission
- [ ] Оптимизации (опционально)

#### 4.2.3. Стандартная библиотека
- [ ] **libkios/std.h** — stdio, stdlib, string
- [ ] **libkios/alloc.h** — аллокаторы
- [ ] **libkios/collections.h** — списки, словари
- [ ] **libkios/io.h** — файловый ввод/вывод

### 4.3. Рантайм

#### 4.3.1. C Runtime
- [ ] **src/rt/crt0.s** — точка входа
- [ ] **src/rt/crt.c** — инициализация
- [ ] `.init` секции

#### 4.3.2. Standard Library Implementation
- [ ] `printf()` / `sprintf()`
- [ ] `malloc()` / `free()` — пользовательский аллокатор
- [ ] `memcpy()`, `memset()`, `strlen()`, `strcmp()`
- [ ] File API: `fopen()`, `fread()`, `fwrite()`, `fclose()`

### 4.4. Userspace

#### 4.4.1. Shell (KiSH)
- [ ] **userspace/kish/main.c**
- [ ] Интерпретатор команд
- [ ] Перенаправление ввода/вывода
- [ ] Конвейеры (pipes)

#### 4.4.2. Базовые приложения
- [ ] **userspace/kshell** — графическая оболочка
- [ ] **userspace/kcalc** — калькулятор
- [ ] **userspace/kedit** — текстовый редактор
- [ ] **userspace/kimg** — просмотр изображений

---

## ⚫ ФАЗА V: Графическая среда

**Цель:** Полноценный графический интерфейс.

### 5.1. Графический сервер

#### 5.1.1. Display Server
- [ ] **src/display/ds.h** — display server API
- [ ] **src/display/ds.c**
- [ ] Window management
- [ ] Input routing (keyboard/mouse → windows)

#### 5.1.2. Compositor
- [ ] Композитинг окон
- [ ] VSync
- [ ] Перерисовка (damage tracking)

### 5.2. GUI Framework

#### 5.2.1. Window System
- [ ] **include/gui/window.h**
- [ ] Заголовки, кнопки, рамки
- [ ] Перетаскивание (drag & drop)
- [ ] Изменение размера

#### 5.2.2. Widgets
- [ ] Button, Label, TextBox
- [ ] ListView, TreeView
- [ ] Menu, ContextMenu
- [ ] ScrollBar

#### 5.2.3. Rendering
- [ ] Software renderer (встроенный)
- [ ] 2D примитивы
- [ ] Текстовый рендеринг (шрифты)

### 5.3. Desktop Environment

#### 5.3.1. KiDesktop
- [ ] Рабочий стол с обоями
- [ ] Панель задач (taskbar)
- [ ] Меню Пуск
- [ ] Системный трей

---

## 🟣 ФАЗА VI: Self-Hosting

**Цель:** KiOS компилирует сама себя.

### 6.1. Toolchain на KiOS

#### 6.1.1. Port GCC/Clang
- [ ] Кросс-компиляция тулчейна
- [ ] or: Написание нативного компилятора на KiLang

#### 6.1.2. Binutils
- [ ] Assembler (as)
- [ ] Linker (ld)
- [ ] Object file tools (objdump, objcopy)

### 6.2. Self-Hosting Pipeline

```
KiOS Source → (KiCC) → KiELF Binary → Запуск на KiOS
```

- [ ] Скомпилировать KiCC на хост-системе
- [ ] Запустить KiCC на KiOS
- [ ] Скомпилировать ядро KiOS на самой себе

---

## 📊 Зависимости между фазами

```
Фаза I (Готово)
    │
    ▼
Фаза II ────────────────┐
    │                   │
    ▼                   ▼
Фаза III        Фаза V
    │                   │
    └─────────┬─────────┘
              ▼
         Фаза IV
              │
              ▼
         Фаза VI
```

---

## 🎯 Quick Wins (Быстрые победы)

Приоритетный порядок для немедленной работы:

1. **VMM** — виртуальная память (Фаза 2.1)
2. **Syscall** — интерфейс ядро/юзер (Фаза 2.2)
3. **AHCI driver** — работа с диском (Фаза 3.1)
4. **KiFS** — файловая система (Фаза 3.2)
5. **KiELF** — формат файлов (Фаза 3.3)

---

## 📝 Чеклист текущей сессии

- [ ] Создать src/vmm.h, src/vmm.c
- [ ] Реализовать базовый paging
- [ ] Интегрировать в kernel.c
- [ ] Протестировать в QEMU
